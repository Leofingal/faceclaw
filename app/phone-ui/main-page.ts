/**
 * The phone's home hub. The page itself is bound to MainViewModel and shares
 * its whole lifecycle with the glasses mirror page; see hub-page.ts.
 */
export { hubPageNavigatingTo as navigatingTo, hubPageLoaded as loaded, hubPageUnloaded as unloaded } from './hub-page'
