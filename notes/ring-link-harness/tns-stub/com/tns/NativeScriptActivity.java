package com.tns;

/**
 * Harness-only: the NativeScript runtime's activity class, which the real
 * build gets from the NativeScript Android runtime. Something in the
 * communicator's compile closure names it (FaceclawTimerNotifications); the
 * ring path never touches it.
 */
public class NativeScriptActivity {
}
